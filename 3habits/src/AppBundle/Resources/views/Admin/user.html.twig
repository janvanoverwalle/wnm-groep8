{% extends "::base.html.twig" %}

{% block title %}Gebruiker {% if user %} {{user.name}} {% else %} niet gekend {% endif %} | Administrator{% endblock %}

{% block body %}
{% if msg %}
    <p>{{ msg }}</p>
{% endif %}

{% if form %}
    {{ form_start(form) }}
        {{ form_errors(form) }}

    {{ form_end(form) }}
{% else %}
    <pre>Deze gebruiker bestaat niet.</pre>
{% endif %}
{% endblock %}
